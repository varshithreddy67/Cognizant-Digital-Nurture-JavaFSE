import { CourseFormatPipe } from './course-format-pipe';

describe('CourseFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
